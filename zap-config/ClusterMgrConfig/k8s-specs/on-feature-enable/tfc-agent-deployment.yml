apiVersion: apps/v1
kind: Deployment
metadata:
  name: feature-deployment-{{ getFeatureSuffix }}
  namespace: cisco-terraform
  labels:
    app: feature
    {{ getFeatureLabel }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feature
  template:
    metadata:
      labels:
        app: feature
    spec:
      restartPolicy: Always
      containers:
      - name: tf-nd-app-tfc-agent
        image: tf-nd-app-tfc-agent:latest
        imagePullPolicy: IfNotPresent
        env:
          - name: TFC_AGENT_TOKEN
            value: {{getConfigParameter "token" }}
          - name: TFC_AGENT_NAME
            value: {{getConfigParameter "name" }}
          - name: http_proxy
            value: {{getConfigParameter "http_proxy"}}
          - name: https_proxy
            value: {{getConfigParameter "https_proxy"}}